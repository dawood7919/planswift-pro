# فحص صحي غير مدمر لـVPS — 19 أغسطس 2026

أجري الفحص باستخدام اتصال SSH مفوض إلى الخادم، من دون تعديل ملفات أو خدمات أو حاويات.

| المجال | النتيجة | الأثر على Takeoff |
|---|---|---|
| هوية الخادم | `VM-0-6-ubuntu`، وقت تشغيل 29 يوماً | الوصول متاح للفحص المرحلي |
| بيئة Takeoff | `/home/ubuntu/takeoff-stage` موجودة وحجمها 462MB | لا تتعارض مع مجلدات المشروع الحالية |
| الخدمات القائمة | حاويتان مستقلتان تعملان: `legal-torrent-bot` و`torrent-search-api` | لا يجوز تغييرها أو استخدام منافذها لتجربة Takeoff |
| المنافذ المستمعة | 22 و8081 و8787 عامة، و9090 محلية | لا يوجد منفذ مرحلي مخصص لـTakeoff بعد |
| سعة القرص | 26GB مستخدمة من 30GB، والمتاح 2.2GB (93%) | لا يُنصح ببناء صور أو تثبيت بيئة تشغيل جديدة قبل تحرير مساحة مدروسة |
| أكبر مسار | `/var` بحجم 22GB | يحتاج تفصيلاً غير مدمر قبل أي تنظيف |

> لم تُجر أي عملية تنظيف أو إعادة تشغيل أو تعديل لجدار الحماية. تجنباً لخطر التأثير على خدمات قائمة، يلزم فحص تفصيلي لمسارات `/var` وتحديد ملفات آمنة للحذف أو نقلها قبل تجهيز تشغيل دائم لـTakeoff.

## تنظيف معتمد من المستخدم

بعد عرض نتائج الفحص، وافق المستخدم صراحةً على تنظيف محافظ يقتصر على Docker build cache غير المستخدمة وسجلات النظام القديمة. لم تمس العملية الحاويتين العاملتين، ولا Docker volumes، ولا مجلدات المشاريع.

| العملية | الأثر |
|---|---|
| `docker builder prune -af` | حرر 10.61GB من ذاكرة بناء Docker غير المستخدمة؛ لم يحذف صوراً عاملة أو حاويات أو volumes. |
| `journalctl --vacuum-time=7d` | حرر 624MB من سجلات systemd المؤرشفة؛ بقي 224MB من السجلات. |
| المساحة بعد التنظيف | 12GB متاحة من 30GB، والاستخدام انخفض من 93% إلى 59%. |
| الحاويات | `legal-torrent-bot` و`torrent-search-api` بقيتا في حالة `Up`. |
| الخدمات المحلية | المنفذ 8787 أعاد `200`، والمنافذ 8081 و9090 تستمعان لكن مسار الجذر يعيد `404`؛ لم يُعد ذلك فشلاً لأن مساراتهما الصحية غير مفحوصة. |

بيئة `/home/ubuntu/takeoff-stage` ما زالت موجودة وبها `package.json`. أصبح هناك هامش كافٍ لبناء وتجربة Takeoff المرحلية، لكن تشغيل التدفقات المحمية يظل متوقفاً على بدائل OAuth وقاعدة البيانات والتخزين الموثقة.

## البنية المرحلية وفحص البناء

حُفظ المصدر الحالي كإصدار مستقل في `/home/ubuntu/takeoff-stage/releases/20260819T113236Z`، وأشار الرابط `current` إليه. أضيف ملف Compose تحت `ops/vps/docker-compose.stage.yml` باسم مشروع `takeoff-stage`، وشبكة مخصصة `takeoff_stage_network`، وvolumes خاصة بأسماء `takeoff_stage_*`. أزيلت الشبكة المؤقتة الفارغة التي أنشأتها المحاولة الأولى للفحص.

نجح profile `validation` في حاوية Node 22 غير جذرية: اكتمل `pnpm check`، ونجحت **23 مجموعة اختبار و75 اختباراً**، واكتمل `pnpm build`. لم تُفتح منافذ ولم تبق حاوية الفحص مرتبطة بالشبكة بعد انتهائها. انخفضت المساحة المتاحة بعد تنزيل صورة Node وتثبيت الاعتمادات إلى 11GB، وهو هامش مناسب للفحوص المرحلية الحالية.

ظهرت تحذيرات بناء غير حاجبة بسبب متغيري التحليلات غير المعرّفين في VPS وحجم حزمة الواجهة؛ لا تمثل فشل البناء. لا يبدأ profile `app` ولا ينشئ المنفذ الحلقي `4179` إلى أن تُوفر بدائل مرحلة مخصصة لـOAuth وقاعدة البيانات والتخزين في ملف الأسرار المحلي.
