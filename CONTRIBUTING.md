# المساهمة في Takeoff Engine Platform

نرحب بالمساهمات التي تحافظ على دقة القياس وقابلية التدقيق. قبل البدء، راجع `docs/software-construction-plan.md` و`docs/phase1-review-notes.md`.

## قواعد العمل

كل تغيير في منطق الهندسة أو المقياس أو الكمية يحتاج اختباراً حتمياً، ولا يجوز إضافة زر أو واجهة توحي بوظيفة غير منفذة. يجب أن تكون الواجهة مستهلكةً لنتيجة النواة، لا منشأً مستقلاً للحساب.

## فحوص مطلوبة

```bash
pnpm check
pnpm test
```

أضف حالات اختبار لأي قاعدة هندسية جديدة، ودوّن أي قيد في الوثائق. لا تضف ملفات رسم أو بيانات عملاء أو أصولاً غير مرخصة إلى المستودع.
